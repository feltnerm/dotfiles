#!/bin/sh
#
# YouTube Playlist Downloader
#
if [ -z $1 ]; then
        echo
        echo "USAGE: youtubepldl [playlist_id]"
        echo
        exit 0;
fi

youtube-dl --extract-audio --audio-quality 320k -t http://www.youtube.com/playlist\?list\=$1
